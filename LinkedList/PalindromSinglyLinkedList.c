#include <stdio.h>
#include <stdlib.h>
#include <string.h>

typedef struct stack // LIFO (Last in First Out)
{
    int data;
    int top;
} stack;

typedef struct Node
{
    int value;
    Node *next;
} Node;

int isPalindrome(struct ListNode *head)
{

    return 1;
}

int main()
{
    return 0;
}