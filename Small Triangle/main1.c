#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int compareString(char *str1, char *str2)
{

    for (char *i = str1; i != NULL; i++)
    {
        char *c = str2
        if (*i = 'a' && )
    }
    return 1;
}

int main()
{
    char str1 = "aba";
    char str2 = "cdc";
    printf("%d", compareString(&str1, &str2));
    return 0;
}
