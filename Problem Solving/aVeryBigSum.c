#include <stdio.h>
#include <stdlib.h>

long long aVeryBigSum(int a[], int n)
{
}

int main()
{
    long a[10];
    int n;
    for (int i = 0; i < n; i++)
    {
        scanf("%long", &a[i]);
    }
    return 0;
}